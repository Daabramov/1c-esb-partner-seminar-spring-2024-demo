Функция ПоместитьОбъектВТелоСообщения(Сообщение, Объект, ПараметрыПреобразования = Неопределено) Экспорт

	Json = КоннекторХТТП.ОбъектВJson(Объект, ПараметрыПреобразования);

	ТелоПоток = Сообщение.ПолучитьТелоКакПоток();
	Запись = Новый ЗаписьДанных(ТелоПоток);

	Запись.ЗаписатьСтроку(Json);
	Запись.Закрыть();
	ТелоПоток.Закрыть();

КонецФункции

Процедура ОбработкаОчередиИнтеграции() Экспорт
	СервисыИнтеграции.ВыполнитьОбработку();
КонецПроцедуры
