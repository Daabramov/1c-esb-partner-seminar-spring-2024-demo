Процедура ПриЗаписи(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Набор = РегистрыСведений.СтатусыКурьерскихЗаявок.СоздатьНаборЗаписей();
	Набор.Отбор.Заявка.Установить(Ссылка);
	Период = ТекущаяДатаСеанса();
	Запись = Набор.Добавить();
	Запись.Заявка = Ссылка;
	Запись.Статус = ДополнительныеСвойства.Статус;
	Запись.Период = Период;
	Набор.Записать();

	КурьерскиеЗаявкиИнтеграция.ОтправитьДанныеПоЗаявке(Ссылка);
КонецПроцедуры
